import './globals.css';
export const metadata = { title: 'Macro Brief', description: 'CTA + Fed H.4.1 dashboard' };
export default function Root({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
